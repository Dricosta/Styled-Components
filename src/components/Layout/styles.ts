import styled from "styled-components";
import { flexColumCenter } from "../../styles/common";

export const Layout = styled.main`
  width: 100%;
  padding-top: 5rem;
  display: flex;
  ${flexColumCenter}
`;

export const LogoIkatec = styled.img`
  width: 8rem;
`;
